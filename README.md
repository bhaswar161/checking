# Studyhub Auth UI

Simple static login/create-account UI with:
- Email + phone collection
- OTP send/verify simulation
- Google/Facebook OAuth-style access prompt

## Run

```bash
python3 -m http.server 8000
```

Open `http://localhost:8000`.
